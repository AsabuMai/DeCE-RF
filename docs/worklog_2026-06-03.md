# Worklog 2026-06-03

## 1. E2 Target-Mode RF Baseline Continuation

Continued E2 under the target-mode protocol:

```text
source image + source prompt + target prompt
no DeCE support mask
no method-specific manual tuning for paper inclusion
```

The strict Core-6 task set remains:

```text
cat_crown
bowl_apple_inside
tshirt_star
red_chair_blue
pillow_vertical_fabric_strip
backpack_remove_toy_charm
```

## 2. Completed Reduced RF Comparison Entries

The reduced target-mode RF comparison now includes three external RF baselines:

```text
FlowEdit
FlowAlign
SplitFlow
```

Strict generation status:

```text
FlowEdit:   18/18 complete
FlowAlign:  18/18 complete
SplitFlow:  18/18 complete
DeCE-RF:    18/18 complete
```

The normalized E2 comparison set contains:

```text
6 tasks x 4 methods x 3 seeds = 72 complete metric rows
```

Artifacts:

```text
outputs/e2_rf_comparison/
experiments/support_v3_2026-06-02/e2_reduced_rf_fixed_mask_metrics.csv
experiments/support_v3_2026-06-02/e2_reduced_rf_fixed_mask_metrics.json
experiments/support_v3_2026-06-02/e2_reduced_rf_comparison_summary.csv
experiments/support_v3_2026-06-02/e2_reduced_rf_comparison_summary.md
```

## 3. Human Visual Audit

Generated five-column E2 grids:

```text
Source | FlowEdit | FlowAlign | SplitFlow | DeCE-RF
```

Grid artifacts:

```text
experiments/support_v3_2026-06-02/visual_audit/e2_flowedit_seed10_grid.png
experiments/support_v3_2026-06-02/visual_audit/e2_flowedit_seed11_grid.png
experiments/support_v3_2026-06-02/visual_audit/e2_flowedit_seed12_grid.png
```

Human quick-audit readout:

```text
FlowEdit:   0/18 pass
FlowAlign:  0/18 pass
SplitFlow:  0/18 pass
DeCE-RF:   18/18 pass
```

Interpretation:

- External target-mode RF baselines often form the requested target object or
  attribute.
- They still frequently redraw source identity, crop, object geometry, or
  background.
- `backpack_remove_toy_charm` remains especially weak for external target-mode
  baselines: the charm is often retained, replaced, or globally redrawn.
- Therefore the external RF outputs are valid reduced-comparison evidence, but
  not visually successful localized-editing examples.

Visual audit artifacts:

```text
experiments/support_v3_2026-06-02/e2_reduced_rf_visual_audit.csv
experiments/support_v3_2026-06-02/e2_reduced_rf_visual_audit.md
```

## 4. Remaining RF Baseline Audit

Strict smoke was attempted for the remaining RF-family baselines:

```text
FireFlow
RF-Solver-Edit
ReFlex
stable-flow
```

Findings:

- FireFlow reached model loading but failed on gated
  `black-forest-labs/FLUX.1-dev` checkpoint access with 401 Unauthorized.
- RF-Solver-Edit reached model loading but failed on the same FLUX.1-dev gated
  checkpoint access.
- ReFlex first failed because the local wrapper passed unsupported
  `--image_size`; the wrapper was fixed.
- ReFlex then failed because `torchao==0.17.0` was incompatible with
  `torch==2.4.1`; the environment was repaired by pinning `torchao==0.5.0`.
- After those fixes, ReFlex also failed on gated FLUX.1-dev access.
- stable-flow requires an authenticated HuggingFace token for FLUX.1-dev and
  has no completed strict adapter output.

Current strict manifest status:

```text
FireFlow:        0 complete, 1 failed, 17 pending
RF-Solver-Edit:  0 complete, 1 failed, 17 pending
ReFlex:          0 complete, 1 failed, 17 pending
stable-flow:     0 complete, 0 failed, 18 pending
```

Audit artifacts:

```text
experiments/support_v3_2026-06-02/e2_strict_rf_baseline_manifest.csv
experiments/support_v3_2026-06-02/e2_baseline_download_registry.csv
experiments/support_v3_2026-06-02/e2_baseline_runnable_validation.csv
experiments/support_v3_2026-06-02/e2_baseline_audit.md
```

## 5. Environment And Quota Notes

The full per-baseline environment setup was initially created, but the server
hit user quota during continued strict generation. To restore write capacity,
non-RF external baseline environments were pruned. The active server keeps the
RF-family environments needed for E2:

```text
flowedit-py310
flowalign-py310
splitflow-py310
fireflow-py310
rf-solver-edit-py310
reflex-py310
stable-flow-py311
```

The ReFlex environment preparation script was updated to pin:

```text
torchao==0.5.0
```

so future rebuilds do not pull a torchao release that requires newer torch
integer dtypes.

## 6. Paper And Claim-Boundary Updates

Updated paper-facing experiment documents:

```text
paper/tables.md
paper/figures.md
paper/wacv_experiment_design.md
```

Current E2 wording:

```text
Reduced target-mode RF comparison against FlowEdit, FlowAlign, and SplitFlow.
```

Do not write:

```text
DeCE-RF beats all RF baselines.
```

Correct claim boundary:

```text
DeCE-RF is compared against three runnable external target-mode RF baselines
under the revised strict Core-6 protocol. Other RF baselines remain in the
baseline audit because strict generation is blocked by gated checkpoint access
or missing strict adapter output.
```

## 7. Code Changes

Updated scripts:

```text
scripts/prepare_e2_reduced_comparison.py
scripts/summarize_e2_reduced_rf.py
scripts/audit_e2_baseline_runnability.py
scripts/prepare_e2_baseline_envs.py
scripts/archive_legacy_2026-05-11/run_reflex_baseline.py
```

Main changes:

- include SplitFlow in reduced-comparison normalization;
- include FlowAlign and SplitFlow in metrics summary, visual audit, and grids;
- record strict complete/failed/pending counts in the E2 baseline audit;
- expose concrete strict failure examples in the audit table;
- fix ReFlex wrapper arguments;
- pin ReFlex `torchao` compatibility.

Validation:

```text
E2 metrics: 72/72 complete
E2 visual audit rows: 72/72 present
old E2 FlowEdit-only wording: removed from paper docs
```

## 8. E2-B Preservation-Aware Baseline Registration

Added the requested preservation-aware / fidelity-oriented RF baseline candidates
to the E2 baseline registry, runnable-validation sheet, audit table, and strict
Core-6 manifest:

```text
rf_solver_edit = RF-Solver-Edit / RF-Edit
ot_rf_otip = OT-RF / OTIP-style
reflex = ReFlex
dvrf = DVRF / Delta Velocity RF
```

Current strict manifest status:

```text
rf_solver_edit: 0 complete, 1 failed, 17 pending
ot_rf_otip:     0 complete, 0 failed, 18 pending
reflex:         0 complete, 1 failed, 17 pending
dvrf:           0 complete, 0 failed, 18 pending
```

RF-Solver-Edit and ReFlex are blocked by gated FLUX.1-dev access. OT-RF / OTIP
and DVRF are registered planned entries and still need exact repo verification,
environment creation, smoke tests, and Core-6 adapters.
