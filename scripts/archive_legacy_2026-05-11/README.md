# Legacy Script Archive

This directory keeps older single-sample probes and early experiment entry
points, mostly from the panda / sunglasses exploration stage.

The current active experiment entry points are:

- `scripts/run_main_table.sh`
- `scripts/run_pretty_matrix.sh`
- `scripts/run_ablation_table.sh`
- `scripts/run_missing_main_matrix.sh`

Use the archived scripts only for traceability or when reconstructing older
debug runs.

