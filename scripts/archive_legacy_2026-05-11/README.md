# Legacy Script Archive

This directory keeps older single-sample probes, baseline wrappers, audit
helpers, paper/workshop builders, and early experiment entry points.

The current active experiment entry point is:

- `scripts/run_pretty_matrix.sh`

Use the archived scripts only for traceability or when reconstructing older
debug runs.
