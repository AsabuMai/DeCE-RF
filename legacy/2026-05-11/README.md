# Legacy Snapshot

This directory contains older code paths, demo assets, and task notes that were
moved out of the active project layout on 2026-05-11.

Archived contents:

- `code/`: early standalone RF pipeline and CLI.
- `h_edit_compare/`: old panda demo asset and config.
- `task/`: previous task planning documents.

The active SD3 RF editing path now lives at the repository root:

- `run_edit_sd3.py`
- `sd3_hrec.py`
- `generic_support.py`

