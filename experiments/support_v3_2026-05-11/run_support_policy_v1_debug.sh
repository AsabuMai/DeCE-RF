#!/usr/bin/env bash
set -euo pipefail

ROOT="${ROOT:-/home/Wu_25R8111/rf_h_edit_project}"
cd "${ROOT}"

TASKS="${TASKS:-P1 P2 P3 P5 P6 P7 P8 P10 P11 P12 P13}"
METHODS="${METHODS:-M17}"
SEEDS="${SEEDS:-10}"
DEVICE="${DEVICE:-7}"
METHOD_NAME_SUFFIX="${METHOD_NAME_SUFFIX:-_policyv1_debug}"
SUPPORT_DEBUG_ONLY="${SUPPORT_DEBUG_ONLY:-1}"
SKIP_EXISTING="${SKIP_EXISTING:-1}"
REGENERATE_MASKS="${REGENERATE_MASKS:-1}"
REUSE_SEMANTIC_MASKS="${REUSE_SEMANTIC_MASKS:-0}"
ALLOW_MASK_DOWNLOAD="${ALLOW_MASK_DOWNLOAD:-0}"
SUPPORT_V3_TEMPORAL_AGGREGATION="${SUPPORT_V3_TEMPORAL_AGGREGATION:-mean}"

export TASKS METHODS SEEDS DEVICE METHOD_NAME_SUFFIX SUPPORT_DEBUG_ONLY
export SKIP_EXISTING REGENERATE_MASKS REUSE_SEMANTIC_MASKS ALLOW_MASK_DOWNLOAD
export SUPPORT_V3_TEMPORAL_AGGREGATION

bash scripts/run_pretty_matrix.sh
